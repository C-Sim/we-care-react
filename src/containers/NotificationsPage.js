// For all user types to view notifications most relevant to them - may need to split into 3, but use first created as template for others

export const NotificationsPage = () => {
  return <h1>NotificationsPage</h1>;
};
